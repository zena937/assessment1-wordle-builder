services:
  app:
    build: .
    container_name: phoneme_app
    ports:
      - "3001:3000"  # ← Use 3002 instead
    environment:
      NODE_ENV: production
      DATABASE_URL: "file:/app/prisma/dev.db"
    volumes:
      - ./prisma:/app/prisma